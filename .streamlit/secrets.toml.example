# Copy to secrets.toml (or run: python scripts/sync_streamlit_secrets.py)
# Paste the generated secrets.toml into Streamlit Cloud → App settings → Secrets.
#
# Service account key (gitignored): .streamlit/filampillardirectory-cb6db0de17be.json

[google_sheets]
sheet_id = "YOUR_GOOGLE_SHEET_ID"

# Filled automatically by scripts/sync_streamlit_secrets.py from the JSON key:
#
# [gcp_service_account]
# type = "service_account"
# project_id = "filampillardirectory"
# private_key_id = "..."
# private_key = "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# client_email = "filampillardirectory-sa@filampillardirectory.iam.gserviceaccount.com"
# client_id = "..."
# auth_uri = "https://accounts.google.com/o/oauth2/auth"
# token_uri = "https://oauth2.googleapis.com/token"
# auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
# client_x509_cert_url = "https://www.googleapis.com/robot/v1/metadata/x509/..."

# Staff login + cookie are added by: make sync-secrets
# Paste the full generated .streamlit/secrets.toml into Streamlit Cloud → Secrets.
#
# [credentials.usernames.FilPilChurch]
# email = "staff@filampillar.org"
# name = "Church Staff"
# password = "bcrypt-hash-from-admin_credentials.toml"
#
# [cookie]
# expiry_days = 1
# key = "random-signing-key"
# name = "church_directory_cookie"
#
# Optional: bootstrap geocode cache for Streamlit Cloud (run: make pregeocode-secrets)
# [geocode_cache]
# "5253 W Byron St, Chicago, IL 60641" = { lat = 41.95, lng = -87.75 }
